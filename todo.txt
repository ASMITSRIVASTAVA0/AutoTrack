token name diff for each role
child can have only one par, so out of all par req, user can accept req only of one, but par can have many childs
chat room creation for par,captain,user
logout route not working currently
ml to decide rating of driver based on comment
ml to get dist between src and dest
GEOAPIFY_API_KEY didnot give res even once

token save in cookies as "user-token", "par-token", "cap-token" so to have all in single browser
check if possible the driver gets verified by car no.(up77xxxx)

make all oper atomic, either full or none

lazy and a global suspense for every and all compo

useMemo for fare calculation

socket.id is inconsitent for all user,captain,par

lenis to make website smooth

only req to backend only from frontend, block postman or else

parent.service.js me addnotifcation ke sath removenotification bhi add, joki 24hr me expire


try to automate starting of both terminal

perform testing(load capacity/numeric data) and add results 

add security to cyber attacks

add videocall facility in which par can do a video call with user

curr project is bad on dark mode


FOR CHATING FEATURE, USE BITCHAT ARCHITECTURE THAT IS USED BY FOUNDER OF TWITTER IN WHICH DATA IS SPLIT INTO TINY PACKETS CALLED LZ4, CREATING MESH LIKE BLUETOOTH NETWORK, MAKE MESSAGE END TO END ENCRPTED, MAKE MESSAGE TIME TO LIVE SO MESSAGE GETS REMOVED BY LET 24 HOURS, SO MAKE MESSAGE STORE ON RAM INSTEAD OF DISK

THINK HOW TO REDUCE NETWORK LATENCY WHEN MAKING MICROSERVICE

frontend deploy at 3 hosts(user,par,captain)

RATE LIMITING TO PREVENT DDOS ATTACK

allow only landing page to req user/par/captain-app and chat 
